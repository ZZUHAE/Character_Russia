---
type: 개요
status: 완성
description: "Vault 전 노트의 frontmatter(메타데이터) 계약. LLM이 본문을 열지 않고도 관련성을 판정·선별 로드하도록 필수 필드·허용값(enum)·표기 규칙을 못박는다. v2에서 집필 레이어(80_집필·90_원고) 계약 추가. lint 스크립트의 근거 문서."
tags: [메타, 표준, frontmatter, LLM위키]
created: 2026-07-07
modified: 2026-07-11
aliases: [frontmatter 표준, 메타데이터 표준, frontmatter standard]
moc: "[[세계관_허브]]"
---

# frontmatter 표준 (v2)

> 이 Vault를 **메타데이터 기반 LLM 위키**로 운용하기 위한 계약.
> LLM은 `description`만 스캔해 노트를 열지 말지 판단하고, `moc`로 소속을 집계하며,
> `status`로 성숙도를 안다. 이 필드들이 깨지면 쿼리·선별 로드가 무너지므로 **스키마는 고정**이고
> `_scripts/frontmatter_lint.py`로 강제한다.

## 1. 내용노트 필수 필드 (8)

`00_세계관개요 · 10_인물 · 20_지역 · 30_세력 · 40_체계 · 50_연표사건 · 60_용어개념`
아래 폴더의 모든 `.md`(단, `_XXX_MOC.md` 제외)는 다음 8개를 **전부** 가진다.

| 필드 | 형식 | 규칙 |
|---|---|---|
| `type` | enum | `인물 / 지역 / 세력 / 체계 / 사건 / 용어 / 세계관개요 / 수치모델` 중 하나. (MOC 파일은 `MOC`) |
| `status` | enum | `씨앗 / 초안 / 구체화 / 완성` 중 하나 |
| `description` | 문자열(따옴표) | **한국어 1~2문장, LLM용 관련성 요약.** §3 규칙 |
| `tags` | 배열 | `[a, b, c]` |
| `created` | `YYYY-MM-DD` | 생성일. 변경 금지 |
| `modified` | `YYYY-MM-DD` | 최종 수정일. 노트 **내용**을 고칠 때마다 갱신 |
| `aliases` | 배열 | 별칭(원어명 포함 가능) |
| `moc` | 위키링크(따옴표) | 소속 MOC. §4 폴더 결정표 |

노트별 선택 필드(`position` `born` `when` `family` …)는 필수 뒤에 자유롭게 유지한다.

## 2. 필드 순서 (권장)

```yaml
type → status → description → tags → created → modified → aliases → moc → (노트별 필드)
```
기존 노트에 소급 적용 시: 기존 필드는 그대로 두고 `description`은 `status` 뒤, `modified`는 `created` 뒤, `moc`는 `aliases` 뒤에 **삽입**한다(전면 재정렬 불필요).

## 3. `description` 작성 규칙 (핵심)

- **한국어 1~2문장.** 본문 `## 개요`(있으면)를 압축해서 쓴다.
- **구체적·명사 중심.** "이 인물에 대한 노트" 같은 **제네릭 금지**. 누가/무엇을/왜 중요한지가 담겨야 한다.
- **위키링크 대괄호 제거.** `[[마리아_돌마토바]]` → `마리아 돌마토바` (본문 아님, 순수 산문).
- **반드시 큰따옴표로 래핑.** `:` `#` `[` `]` 등 특수문자가 YAML 파서를 깨뜨리는 걸 막는다.
- 내부 큰따옴표는 피하고(다시 쓰기) 어쩔 수 없으면 `\"` 이스케이프.

예)
```yaml
description: "페테르부르크 명문 돌마토프 가문의 거상 출신 개혁가. 중공업·군수 생산력을 지렛대로 정계에 올라 1915년 제국 내각 부총리(실권은 그 이상). 개혁파의 동력이자 황실파의 최대 정적."
```

## 4. `moc` 폴더 결정표

| 폴더 | `moc` 값 |
|---|---|
| `00_세계관개요` | `"[[_개요_MOC]]"` |
| `10_인물` | `"[[_인물_MOC]]"` |
| `20_지역` | `"[[_지역_MOC]]"` |
| `30_세력` | `"[[_세력_MOC]]"` |
| `40_체계` | `"[[_체계_MOC]]"` |
| `50_연표사건` | `"[[_연표_MOC]]"` |
| `60_용어개념` | `"[[_용어_MOC]]"` |

MOC 파일 자신(`_XXX_MOC.md`)과 허브(`세계관_허브`)는 `moc`를 갖지 않는다(그 자체가 색인). 단 `description`·`modified`는 갖는다.

## 5. MOC/허브 파일 필드

`_XXX_MOC.md`, `_MOC/세계관_허브.md` 등 구조 노트는 최소:
`type` · `tags` · `description`(이 색인이 무엇을 모으는지) · `modified`.

## 6. 검증

`_scripts/frontmatter_lint.py` 가 전수 검사:
필수 8필드 존재 / `type`·`status` enum / `description` 비어있지 않음·따옴표 / `moc` 값이 실재 MOC 파일로 해결 / YAML 위키링크 따옴표 여부.
→ 노트 추가·수정 후 이 스크립트로 **고아·스키마 위반 0** 을 확인한다.

## 7. 집필 레이어 계약 (v2, 2026-07-11 소설 전환)

`80_집필`(설계) · `90_원고`(산문) 폴더 — **하위 폴더 포함**(스레드/·인물_아크/·부 폴더) 재귀 적용.

| 항목 | 규칙 |
|---|---|
| `type` | `집필`(설계 문서: 규칙·바이블·플롯·스레드·아크) 또는 `원고`(회차 본문) |
| `status` | `집필` → 기존 4단계(`씨앗/초안/구체화/완성`) · **`원고` → 집필 트랙 `구상/초고/퇴고/탈고`** |
| `moc` | 둘 다 `"[[_집필_MOC]]"` (파일 위치: `80_집필/_집필_MOC.md`) |
| 원고 선택 필드 | `when`(작중 시점) · `pov`(초점 인물 배열) · `threads`(관련 스레드 배열) |

나머지 필수 8필드·description 규칙(§3)은 내용노트와 동일. 템플릿: `_템플릿/회차.md` · `스레드.md` · `인물_아크.md`.

## 8. 인물 아크 계약 (v3, 2026-07-15 살아있는 원장 체계)

`80_집필/인물_아크/{인물}_아크.md` — 소설 진행에 따라 갱신되는 인물 상태 노드. canon(10_인물, 동결)의 state 짝.

| 항목 | 규칙 |
|---|---|
| `type` | `인물아크` (WRITING_TYPE_ENUM) |
| `status` | **아크 트랙 `가동 / 퇴장 / 사망`** (설정·원고 트랙과 별개, 3번째 트랙) |
| `character` | canon 역링크 `"[[인물명]]"` — 실재 `10_인물` 파일로 해결돼야 함(lint 강제) |
| `tier` | `주역`(7섹션 전부) 또는 `조연`(①스냅샷·⑤로그만 필수) |
| `last_ep` | 정수. **마지막 반영 확정회차** — lint가 최신 확정회차(퇴고/탈고 최대)와 비교해 `−{STALE_GAP}` 초과 지연 시 **stale 위반** |
| `threads` | 배열(러시아_내정/동아시아/동부전선) |
| `moc` | `"[[_집필_MOC]]"` |

나머지 필수 8필드 동일. lint: type/status/tier enum · last_ep 정수·stale · character 해결 검증.

**canon 인물 노트의 선택 필드 `arc`**: `10_인물/{인물}.md` frontmatter에 `arc: "[[{인물}_아크]]"` 1줄을 **선택적으로** 추가(설계도→궤적 포인터). additive이며 본문·동결과 무관. 없어도 lint 통과.

---
〔편집 메타 OOC〕 이 문서는 canon(세계관 내용)이 아니라 Vault 운영 규칙이다. 배경 동결과 무관하게 갱신 가능. 관련: [[_네모_워크플로]] · [[세계관_허브]]
